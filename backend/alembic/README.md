# A generic Alembic environment can be initialized with:
#   alembic init backend/alembic
# For this project, SQLAlchemy `Base.metadata.create_all` is used on startup
# for rapid local demos. Prefer Alembic migrations before production deploy.
